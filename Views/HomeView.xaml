<Window x:Class="Eindopdracht.Views.HomeView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Eindopdracht.Views"
        xmlns:VM="clr-namespace:Eindopdracht.ViewModels"
        mc:Ignorable="d"
        Title="HomeView" Height="450" Width="800">
    <Window.DataContext>
        <VM:HomeViewModel x:Name="VMHome"></VM:HomeViewModel>
    </Window.DataContext>
    <Grid>
        <ListBox d:ItemsSource="{d:SampleData ItemCount=5}" Width="150" Margin="10,67,640,67" Height="300"/>
        <Label Content="Items" HorizontalAlignment="Left" Margin="10,36,0,0" VerticalAlignment="Top" Width="150" FontWeight="Bold"/>
        <Button Content="Item toevoegen" Command="{Binding AddItemCommand}" HorizontalAlignment="Left" Margin="10,372,0,0" VerticalAlignment="Top"/>


    </Grid>
</Window>
